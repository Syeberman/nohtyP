The files in this directory come from Python's unit test suite, modified to test nohtyP via yp.py.

New files should be added via the vendor-cpython branch, which should exactly match their original 
Python counterparts to ease future integrations.

